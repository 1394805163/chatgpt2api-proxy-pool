# v1.6 选择性升级设计

## 目标

以官方 `v1.6.0` 为升级来源，将本地尚未具备且确实更优的能力移植到定制分支，同时保留注册机、Sentinel SDK、并发生图、持久化、额度控制及账号池策略等本地增强。

## 版本边界

- 当前本地基线：`1.5.0 番外篇3.0`。
- 目标标识：`1.6.0 番外篇4.0`。
- 官方 `v1.7.0` 删除注册模块，不进入本次升级范围。
- 不直接 merge 或 rebase 官方分支，所有改动按行为逐项移植。

## 官方 v1.6.0 逐提交审计

| 官方提交 | 结论 | 本分支处理 |
| --- | --- | --- |
| `f724f8c` 注册邮箱 API 代理 | 保留本地 | 本地已有可配置开关、注册代理透传和对应测试，覆盖相同能力。 |
| `1882bb5` CloudMail token 刷新 | 保留本地 | 本地已覆盖 `addUser`、`emailList` token 失效刷新及短重试。 |
| `d8c88d4` 导入导出账号 JSON | 适配采用 | 由 `f693c95` 接入纯解析函数，并保留 CPA 与后台刷新流程。 |
| `8f4d8fd` Session 资源释放 | 扩展采用 | 由 `3f9c10e` 覆盖官方调用点，并补齐搜索、Web 工具与异常路径。 |
| `4fa120b` 推理强度透传 | 保留本地 | 本地已覆盖 Chat Completions、Responses、缓存键和调试页。 |
| `574d11b` 静态资源 HEAD | 保留本地 | 当前通配路由同时支持 `GET`、`HEAD`，并已有更完整的图片及缩略图 HEAD 处理。 |
| `5d83d6d` Sub2API 多账号 token | 扩展采用 | 由 `f693c95` 改用批量 `accounts/data`，支持部分成功和缺失 token 汇总。 |
| `6dd5552` 邮件时间边界与 addUser | 适配采用 | 由 `7f3e9bf` 接入 UTC 邮件边界与 CloudMailGen 用户登记。 |
| `55cac94` 出图后隐藏上游对话 | 安全适配 | 由 `a50b6e2` 使用独立 Session 异步隐藏，避免复用已关闭的生图客户端。 |
| `eabfd11` 前序空结果队列修复 | 不移植 | 本地 `457bd1c` 已按 Turn 独立并发，并有后端硬时限与槽位释放，覆盖更完整。 |
| `3f7c8a0` Pro 额度统一有限化 | 不移植 | 会把上游未知额度误判为零额度；保留本地 `image_quota_unknown` 策略。 |
| `66fee0c` 版本与日志 | 本地发布 | 使用 `1.6.0 番外篇4.0` 和本地变更日志，不覆盖定制版本信息。 |

## 采用的官方能力

### Session 生命周期

为 `OpenAIBackendAPI` 增加幂等 `close()` 和上下文管理协议，并在账号刷新、文字流、搜索、模型列表、图片任务、可编辑文件任务等短生命周期调用点保证释放。流式响应必须先消费或关闭，再释放 Session。

### 账号 JSON 导入

账号导入页同时支持单账号对象、账号数组，以及 `accounts` 或 `items` 包装结构。保留本地现有的异步刷新进度、重复账号跳过和 CPA 兼容逻辑。

### Sub2API 批量导入

列表展示不再依赖列表响应里存在 access_token；导入改用 `/api/v1/admin/accounts/data?ids=...` 一次导出所选账号，并保留后台任务状态及逐账号错误汇总。

### 邮箱验证码边界

邮箱创建时记录验证码接收起点，忽略更早的历史邮件。CloudMailGen 创建地址时调用 `addUser`，确保后端已登记收件地址。

### 上游对话清理

增加“生图完成后隐藏 ChatGPT 上游对话”开关，默认关闭。仅隐藏 ChatGPT 上游会话，不删除本地图片、系统日志、浏览器历史或数据库记录。异步清理线程使用独立 `OpenAIBackendAPI`，并在成功或失败后关闭自己的 Session，避免与生图客户端关闭流程产生竞态。

## 保留的本地实现

- 推理强度透传已覆盖 Chat Completions、Responses、缓存键和调试页。
- 注册邮箱代理与 CloudMailGen token 失效刷新已实现。
- Web、图片和缩略图的 HEAD 路由覆盖比官方更完整。
- Pro/ProLite 未知额度保持可用，不采用官方统一有限额度策略。
- Sentinel SDK、SO token、5000 ms observer、注册 authorize/continue 保持不变。
- 生图总时限、任务 ID、并发请求、图片持久化、日志持久化和用户额度保持不变。
- 图片队列继续使用本地按 turn 并发实现；官方 `eabfd11` 面向旧串行队列，不移植。

## 验证

- Python 单元测试覆盖 Session 释放、Sub2API 批量导入和邮箱验证码边界。
- Bun 单元测试覆盖账号 JSON 解析。
- 运行全部 Python 测试、Web 构建、Docker 镜像构建。
- 使用新镜像启动隔离容器，验证健康检查、模型接口和静态页面。
